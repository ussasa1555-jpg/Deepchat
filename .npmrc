# Suppress legacy peer dependencies warnings
legacy-peer-deps=false

# Suppress deprecated package warnings
audit-level=moderate

# Use npm ci for faster, reliable builds
prefer-offline=true

# Suppress engine warnings
engine-strict=false